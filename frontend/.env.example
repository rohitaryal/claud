# API URL for backend - leave empty to auto-detect based on window.location.hostname
# If not set, will use current hostname with port 3000 (e.g., http://localhost:3000 or http://0.0.0.0:3000)
# Uncomment and set this only if you need a different backend URL
# VITE_API_URL=http://localhost:3000
